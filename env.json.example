{
  "_comment": "Copy to env.json (git-ignored). Used by: sam local invoke / sam local start-api --env-vars env.json",

  "Parameters": {
    "ENVIRONMENT": "dev",
    "FROM_EMAIL": "noreply@cdmxtraffic.com",

    "SUBSCRIBERS_TABLE": "dev-cdmx-traffic-subscribers",
    "NEWSLETTER_BUCKET": "dev-cdmx-traffic-newsletters",

    "ZAVU_API_KEY_SECRET": "dev/cdmx-traffic/zavu-api-key",
    "OPENAI_API_KEY_SECRET": "dev/cdmx-traffic/openai-api-key"
  }
}
